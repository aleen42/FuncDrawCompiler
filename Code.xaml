<Window x:Class="FuncDraw_WPF_.Code"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True" WindowStyle="None"
        Title="Code" Height="257.721" Width="212.5" Background="#CC4F3050">
    <Grid>
        <TextBox x:Name="Code_text" AcceptsReturn="True" HorizontalAlignment="Left" Height="177" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="192" Opacity="0.8"/>
        <Button Content="Certain" HorizontalAlignment="Left" Margin="30,207,0,0" VerticalAlignment="Top" Width="148" Height="41" FontSize="16" Foreground="#FF4F3050"  Background="#FFFFAB89" Click="Button_Click">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Teacher" 
        				BorderBrush="#FFDF8679"
        				BorderThickness="1,1,5,5"
    					
        				Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>

                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
